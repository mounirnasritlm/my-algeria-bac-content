{
  "subject_id": "french",
  "relationships": [],
  "rules": {
    "correction_linking": "A correction is a separate resource when the source is separate. Link via correction_document_id. If embedded, use correction_pages.",
    "exercise_to_correction": "exercise.document_id → correction.correction_document_id",
    "exam_to_correction": "exam.document_id → correction.correction_document_id",
    "bac_to_correction": "bac.document_id → correction.correction_document_id",
    "never_fake_corrections": "correction_document_id must never be set to a fabricated value. null if no correction exists."
  }
}
